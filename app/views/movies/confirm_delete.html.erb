<div style="text-align: center; margin: 50px; font-family: 'Russo One', sans-serif;">
  <h1>Are you sure you want to delete this movie?</h1>
  <p style="margin: 20px; font-size: 18px;">This action cannot be undone.</p>


  <div style="margin-top: 20px;">
    <%= image_tag @movie.image, size: "250x400", alt: @movie.name if @movie.image.attached? %>
  </div>

  <div style="margin-top: 30px;">
    <%= button_to "Delete", character_movie_path(@character, @movie), method: :delete, class: "btn btn-danger", style: "margin-right: 20px;" %>
    <%= link_to "Cancel", character_path(@character), class: "btn btn-secondary" %>
  </div>
</div>
